// NAME


// DESCRIPTION


// RETURN VALUES

#include "libft.h"

char *ft_strdup(const char *s1)
{
    char *dup;
    size_t str_len = ft_strlen(s1);
    dup = malloc(str_len + 1);
    if (!dup)
        return (NULL);
    ft_strcpy(dup, s1, str_len + 1);
    return (dup);
}